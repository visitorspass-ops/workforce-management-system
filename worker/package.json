{
  "name": "wfm-ingestion-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Always-on HTTP worker that runs the DuckDB ingestion pipeline when Supabase's ingestion_jobs webhook fires. Deployed separately from the Next.js app — see README.md.",
  "scripts": {
    "start": "tsx server.ts",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "duckdb": "^1.1.1",
    "xlsx": "^0.18.5",
    "tsx": "^4.19.1"
  },
  "devDependencies": {
    "@types/node": "^20.16.10",
    "typescript": "^5.6.3"
  }
}
